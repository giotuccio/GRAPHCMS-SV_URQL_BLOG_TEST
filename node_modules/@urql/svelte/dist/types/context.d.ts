import { Client, ClientOptions } from '@urql/core';
export declare const getClient: () => Client;
export declare const setClient: (client: Client) => void;
export declare const initClient: (args: ClientOptions) => Client;
